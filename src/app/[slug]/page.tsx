import Link from 'next/link';
import { notFound } from 'next/navigation';
import { createClient } from '@/lib/supabase/server';
export default async function CMSPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const db=createClient();const {data}=await db.from('pages').select('title,content,seo_title,seo_description,published').eq('slug',slug).maybeSingle();if(!data||!data.published)notFound();const c=(data.content??{}) as any;return <main className="min-h-screen bg-[#f7f5ef]"><div className="bg-[#064e3b] py-3 text-center text-xs text-white">Al Huda Quran Memorization School</div><div className="mx-auto max-w-5xl px-5 py-14"><Link href="/" className="text-sm font-bold text-emerald-700">← Home</Link><div className="mt-8 rounded-3xl bg-white p-7 shadow-sm md:p-12"><div className="text-xs font-bold uppercase tracking-[.22em] text-emerald-700">Al Huda</div><h1 className="mt-2 font-serif text-4xl font-black text-[#064e3b]">{data.title}</h1>{c.intro&&<p className="mt-5 text-lg leading-8 text-slate-600">{c.intro}</p>}{Array.isArray(c.sections)&&<div className="mt-10 space-y-8">{c.sections.map((s:any,i:number)=><section key={i}><h2 className="text-2xl font-black text-slate-900">{s.title}</h2><p className="mt-2 whitespace-pre-line text-sm leading-7 text-slate-600">{s.text}</p></section>)}</div>}{c.cta&&<Link href={c.cta_href??'/admissions'} className="btn btn-primary mt-8">{c.cta} →</Link>}</div></div></main>}
