'use client';
import AdminShell from '@/components/AdminShell';
import {defaultHomepage,loadHomepage,loadTeam,saveHomepage,loadAlumni,saveAlumni,HomepageConfig,TeamProfile,AlumniProfile} from '@/lib/cms-store';
import {useEffect,useState} from 'react';
import type {ChangeEvent} from 'react';

const sectionNames=['Hero banner','About the school','Quran programme','Day & Boarding','Leadership & Management','Our Staff','Admissions','News & announcements','Gallery','Testimonials','Alumni'];
const sectionKey='dq-cms-sections-v6';
export default function CMS(){
 const [config,setConfig]=useState<HomepageConfig>(defaultHomepage); const [team,setTeam]=useState<TeamProfile[]>([]); const [alumni,setAlumni]=useState<AlumniProfile[]>([]); const [sections,setSections]=useState(sectionNames); const [visible,setVisible]=useState<Record<string,boolean>>({}); const [editing,setEditing]=useState<TeamProfile|null>(null); const [saved,setSaved]=useState(false);
 useEffect(()=>{setConfig(loadHomepage());setTeam(loadTeam());setAlumni(loadAlumni());try{const raw=localStorage.getItem(sectionKey);if(raw)setSections(JSON.parse(raw));}catch{}},[]);
 const persist=()=>{saveHomepage(config);saveTeam(team);saveAlumni(alumni);localStorage.setItem(sectionKey,JSON.stringify(sections));setSaved(true);setTimeout(()=>setSaved(false),1800)};
 const move=(i:number,d:number)=>{const j=i+d;if(j<0||j>=sections.length)return;const a=[...sections];[a[i],a[j]]=[a[j],a[i]];setSections(a)};
 const add=()=>{const n:TeamProfile={id:'staff-'+Date.now(),category:'staff',name:'New Staff Member',role:'Role title',bio:'Brief biography',full:'Full profile information',photo:'',home:false,published:false};setTeam(x=>[...x,n]);setEditing(n)};
 const upload=(e:ChangeEvent<HTMLInputElement>,item:TeamProfile)=>{const file=e.target.files?.[0];if(!file)return;const reader=new FileReader();reader.onload=()=>setEditing({...item,photo:String(reader.result)});reader.readAsDataURL(file)};
 return <AdminShell title="Website CMS"><div className="space-y-6">
  <div className="card p-5"><div className="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><div><h2 className="font-bold">Homepage Builder</h2><p className="mt-1 text-xs text-slate-500">Edit, hide, reorder and publish homepage content without changing code.</p></div><div className="flex gap-2"><span className="pill bg-emerald-50 text-emerald-700">{saved?'Saved':'Draft changes'}</span><button onClick={persist} className="btn btn-green">Save & Publish</button></div></div>
   <div className="mt-5 grid gap-4 lg:grid-cols-2"><label className="text-sm font-semibold">School name<input className="input mt-1" value={config.schoolName} onChange={e=>setConfig({...config,schoolName:e.target.value})}/></label><label className="text-sm font-semibold">Tagline<input className="input mt-1" value={config.tagline} onChange={e=>setConfig({...config,tagline:e.target.value})}/></label><label className="text-sm font-semibold lg:col-span-2">Hero title<input className="input mt-1" value={config.heroTitle} onChange={e=>setConfig({...config,heroTitle:e.target.value})}/></label><label className="text-sm font-semibold lg:col-span-2">Hero subtitle<textarea className="input mt-1" value={config.heroSubtitle} onChange={e=>setConfig({...config,heroSubtitle:e.target.value})}/></label><label className="text-sm font-semibold lg:col-span-2">Hero image URL<input className="input mt-1" value={config.heroImage} onChange={e=>setConfig({...config,heroImage:e.target.value})}/></label><label className="text-sm font-semibold">About heading<input className="input mt-1" value={config.aboutTitle} onChange={e=>setConfig({...config,aboutTitle:e.target.value})}/></label><label className="text-sm font-semibold">About text<textarea className="input mt-1" value={config.aboutText} onChange={e=>setConfig({...config,aboutText:e.target.value})}/></label></div>
  </div>
  <div className="card p-5"><h2 className="font-bold">Homepage sections</h2><p className="mt-1 text-xs text-slate-500">This is the order the public homepage uses. In production the same data lives in the CMS database.</p><div className="mt-5 space-y-3">{sections.map((x,i)=><div className="flex items-center gap-3 rounded-xl border p-3" key={x}><div className="flex gap-1"><button onClick={()=>move(i,-1)} className="rounded bg-slate-100 px-2 py-1 text-xs">↑</button><button onClick={()=>move(i,1)} className="rounded bg-slate-100 px-2 py-1 text-xs">↓</button></div><div className="flex-1"><b>{x}</b><div className="text-xs text-slate-500">Section {i+1}</div></div><button onClick={()=>setVisible(v=>({...v,[x]:!(v[x]??true)}))} className="btn bg-slate-100">{(visible[x]??true)?'Visible':'Hidden'}</button><button onClick={()=>alert(`Open the full editor for ${x}`)} className="btn bg-slate-100">Edit</button></div>)}</div></div>
  <div className="card p-5"><div className="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><div><h2 className="font-bold">Leadership, Management & Staff</h2><p className="mt-1 text-xs text-slate-500">Upload pictures, edit bios, manage full profiles and choose who appears on the homepage.</p></div><button onClick={add} className="btn btn-green">+ Add profile</button></div><div className="mt-5 grid gap-4 md:grid-cols-2 lg:grid-cols-3">{team.map(t=><div className="rounded-2xl border p-4" key={t.id}><div className="mb-4 flex h-32 items-center justify-center overflow-hidden rounded-xl bg-slate-100 text-xs text-slate-400">{t.photo?<img src={t.photo} alt={t.name} className="h-full w-full object-cover"/>:'No photo'}</div><div className="text-xs font-bold uppercase tracking-wide text-slate-500">{t.category}</div><h3 className="mt-1 font-bold">{t.name}</h3><p className="text-sm font-medium">{t.role}</p><p className="mt-2 text-xs text-slate-500">{t.bio}</p><div className="mt-4 flex flex-wrap gap-2"><button className="btn bg-slate-100" onClick={()=>setEditing(t)}>Edit</button><button className="btn bg-slate-100" onClick={()=>setTeam(x=>x.map(v=>v.id===t.id?{...v,home:!v.home}:v))}>{t.home?'Remove home':'Show home'}</button><button className="btn bg-rose-50 text-rose-700" onClick={()=>setTeam(x=>x.filter(v=>v.id!==t.id))}>Delete</button></div></div>)}</div></div>
  <div className="card p-5"><div className="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><div><h2 className="font-bold">Alumni publishing</h2><p className="mt-1 text-xs text-slate-500">Choose which graduates are public and which appear on the homepage. Their full academic history remains in the private alumni record.</p></div><button className="btn btn-green" onClick={()=>setAlumni(x=>[...x,{id:'al-'+Date.now(),name:'New Graduate',cohort:'Cohort 2026',brief:'Graduate profile',photo:'',published:false,home:false}])}>+ Add alumni</button></div><div className="mt-5 grid gap-4 md:grid-cols-3">{alumni.map(a=><div className="rounded-2xl border p-4" key={a.id}><div className="font-bold">{a.name}</div><div className="text-xs text-slate-500">{a.cohort}</div><p className="mt-2 text-sm text-slate-600">{a.brief}</p><div className="mt-3 flex gap-2"><button className="btn bg-slate-100" onClick={()=>setAlumni(x=>x.map(v=>v.id===a.id?{...v,published:!v.published}:v))}>{a.published?'Unpublish':'Publish'}</button><button className="btn bg-slate-100" onClick={()=>setAlumni(x=>x.map(v=>v.id===a.id?{...v,home:!v.home}:v))}>{a.home?'Remove home':'Show home'}</button><button className="btn bg-rose-50 text-rose-700" onClick={()=>setAlumni(x=>x.filter(v=>v.id!==a.id))}>Delete</button></div></div>)}</div></div><div className="card p-5"><h2 className="font-bold">Global CMS controls</h2><div className="mt-4 grid gap-3 md:grid-cols-3">{['School identity & branding','Logo / favicon','Homepage SEO','Navigation','Contact details','Social links','Footer','Media library','Announcements','Events','Gallery','Testimonials','Alumni publishing','Admissions content','Fee information'].map(x=><button key={x} onClick={()=>alert(`Open CMS editor: ${x}`)} className="rounded-xl border p-4 text-left text-sm font-semibold hover:bg-slate-50">{x}<span className="float-right">→</span></button>)}</div></div>
  {editing&&<div className="fixed inset-0 z-50 flex items-center justify-center bg-black/50 p-4"><div className="max-h-[90vh] w-full max-w-2xl overflow-auto rounded-2xl bg-white p-6"><div className="flex justify-between"><h2 className="text-xl font-black">Edit public profile</h2><button onClick={()=>setEditing(null)}>✕</button></div><div className="mt-5 grid gap-4 md:grid-cols-2"><label className="text-sm font-semibold">Full name<input className="input mt-1" value={editing.name} onChange={e=>setEditing({...editing,name:e.target.value})}/></label><label className="text-sm font-semibold">Role<input className="input mt-1" value={editing.role} onChange={e=>setEditing({...editing,role:e.target.value})}/></label><label className="text-sm font-semibold">Category<select className="input mt-1" value={editing.category} onChange={e=>setEditing({...editing,category:e.target.value as TeamProfile['category']})}><option value="leadership">Leadership</option><option value="management">Management</option><option value="staff">Staff</option></select></label><label className="text-sm font-semibold">Upload picture<input type="file" accept="image/*" className="mt-2 block w-full text-sm" onChange={e=>upload(e,editing)}/></label><label className="text-sm font-semibold md:col-span-2">Brief bio<textarea className="input mt-1" value={editing.bio} onChange={e=>setEditing({...editing,bio:e.target.value})}/></label><label className="text-sm font-semibold md:col-span-2">Full profile<textarea className="input mt-1 min-h-32" value={editing.full} onChange={e=>setEditing({...editing,full:e.target.value})}/></label><label className="flex items-center gap-2 text-sm font-semibold"><input type="checkbox" checked={editing.published} onChange={e=>setEditing({...editing,published:e.target.checked})}/> Published publicly</label><label className="flex items-center gap-2 text-sm font-semibold"><input type="checkbox" checked={editing.home} onChange={e=>setEditing({...editing,home:e.target.checked})}/> Show on homepage</label></div><div className="mt-5 flex justify-end gap-2"><button className="btn bg-slate-100" onClick={()=>setEditing(null)}>Cancel</button><button className="btn btn-green" onClick={()=>{setTeam(x=>x.map(t=>t.id===editing.id?editing:t));setEditing(null)}}>Save profile</button></div></div></div>}
 </div></AdminShell>
}
