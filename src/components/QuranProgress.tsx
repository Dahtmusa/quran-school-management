'use client';
import {Student,studentStats} from '@/lib/data';
import {juzForPosition} from '@/lib/quran';
export default function QuranProgress({student,compact=false}:{student:Student;compact?:boolean}){const s=studentStats(student);return <div className="card p-5"><div className="flex flex-col gap-2 sm:flex-row sm:items-start sm:justify-between"><div><div className="text-xs font-bold uppercase tracking-wide text-slate-500">Quran memorization progress</div><h3 className="mt-1 text-xl font-black">{s.percent.toFixed(1)}% complete</h3><p className="text-xs text-slate-500">{student.direction} · Start: {s.startLabel}</p></div><div className="rounded-full bg-emerald-50 px-3 py-1 text-xs font-bold text-emerald-700">{s.percent>=100?'Completed':'In progress'}</div></div><div className="mt-4 h-4 overflow-hidden rounded-full bg-slate-100"><div className="h-full rounded-full bg-gradient-to-r from-emerald-700 via-teal-500 to-amber-400 transition-all" style={{width:`${s.percent}%`}}/></div><div className="mt-2 flex justify-between text-xs font-semibold text-slate-500"><span>{s.percent.toFixed(1)}%</span><span>{s.percent>=100?'Quran completed':`${s.remaining.ayahs.toLocaleString()} ayahs · ${s.remaining.pages} pages · ${s.remaining.hizbs} Hizb remaining`}</span></div><div className="mt-4 grid grid-cols-2 gap-2 md:grid-cols-4"><div className="rounded-xl bg-slate-50 p-3"><div className="text-[11px] text-slate-500">Current position</div><b>{s.currentLabel}</b></div><div className="rounded-xl bg-slate-50 p-3"><div className="text-[11px] text-slate-500">Mushaf page</div><b>{s.currentPage} / 604</b></div><div className="rounded-xl bg-slate-50 p-3"><div className="text-[11px] text-slate-500">Current Juz / Hizb</div><b>{juzForPosition(student.current)} / {s.currentHizb} / 60</b></div><div className="rounded-xl bg-slate-50 p-3"><div className="text-[11px] text-slate-500">Remaining</div><b>{s.remaining.pages} pages · {s.remaining.hizbs} Hizb</b></div></div>{!compact&&<div className="mt-4 grid grid-cols-3 gap-2 text-center text-xs"><div className="rounded-xl border p-3"><b className="block text-lg">{s.ayahs.toLocaleString()}</b> Ayahs covered</div><div className="rounded-xl border p-3"><b className="block text-lg">{s.pages}</b> Pages covered</div><div className="rounded-xl border p-3"><b className="block text-lg">{s.hizbs}</b> Hizbs covered</div></div>}</div>}
